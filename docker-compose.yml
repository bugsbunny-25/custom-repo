version: '3.8'

services:
  fdroid-repo:
    image: ghcr.io/bugsbunny-25/custom-repo:main
    container_name: fdroid-repo
    restart: unless-stopped

    # No container-wide mem_limit: the admin-server JVM caps its own heap via
    # -Xmx512m in supervisord.conf, so it doesn't need a cgroup backstop to
    # stay small. The APK patch/sign pipeline runs in a separate short-lived
    # JVM per job (spawned by PatchWorkerLauncher, no -Xmx of its own) that
    # needs however much memory a given APK's patch run takes - a container
    # limit here would just recreate the same OOM risk one level up. If you
    # want a hard ceiling on total container RAM, set mem_limit here based on
    # your host's headroom (a patch run's peak usage depends on APK size and
    # the patches applied - profile a real run rather than guessing).

    volumes:
      # Persist the F-Droid repository data
      - ${CONFIG_FOLDER}/fdroid-data/:/srv/fdroid

      # Persist the SQLite database (all app settings, GitHub repos, patch
      # library/targets, checked-release history). Everything is configured
      # through the admin UI's Settings page after first run - there's no
      # config.yml to hand-edit anymore.
      - ${CONFIG_FOLDER}/db:/app/db

      # Optional: persist logs
      - ${CONFIG_FOLDER}/logs:/var/log

    environment:
      - TZ=UTC
      # Root log level for the app (logback.xml reads this, defaulting to
      # INFO if unset). One of ERROR, WARN, INFO, DEBUG, TRACE.
      - LOG_LEVEL=INFO
      # Override for a different DB directory; defaults to /app/db (the
      # app.db file is created inside it automatically if missing)
      # - DB_PATH=/app/db
    
    # Expose port (will be proxied by your nginx reverse proxy)
    ports:
      - "8080:80"  # Change 8080 to your preferred port
    
    # Health check
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
